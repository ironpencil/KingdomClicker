<%= form_with(model: kingdom, local: true) do |form| %>
  <% if kingdom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kingdom.errors.count, "error") %> prohibited this kingdom from being saved:</h2>

      <ul>
      <% kingdom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :kingdom_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :kingdom_description %>
  </div>

  <div class="field">
    <%= form.label :gold %>
    <%= form.text_field :gold, id: :kingdom_gold %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
